library(shiny)

shinyUI(fluidPage(
  
  # Application title
  titlePanel("Word Prediction App"),
  
  sidebarLayout(
    sidebarPanel(
      # Put the instructions here
    ),
    mainPanel(
      # Input text box
      
      # Prediciton text
    )
  )
))
